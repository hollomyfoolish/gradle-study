task distribution << {
    println "We build the zip with version=$version"
}

task release(dependsOn: 'distribution') << {
    def _allen = 'done'
    println "We release now $_allen"
}

gradle.taskGraph.whenReady {taskGraph ->
    if (taskGraph.hasTask(release)) {
        version = '1.0'
        def _allen = 'done'
    } else {
        version = '1.0-SNAPSHOT'
    }
}